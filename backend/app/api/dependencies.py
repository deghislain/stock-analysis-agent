"""
FastAPI dependency injection helpers shared across all route modules.

Full implementation is added in Sub-Task 5 when the job store and
orchestrator are wired into the API. This stub ensures the module
exists and is importable from the start.
"""

from fastapi import APIRouter  # noqa: F401 — imported here to keep the stub valid
